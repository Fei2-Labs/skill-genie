# Agents

Records of agents I created across agent environments.

Each agent gets one Markdown file named after the agent.

Supported environments include Claude Code, Codex, Kiro, Copilot, and other agent runtimes that can read this repo.

# Session and sync

- Read project AGENTS.md first.
- If an `upstream` remote exists, fetch and report new commits at session start.
- Never push unless explicitly asked.
- Ask one minimal question before irreversible operations.

# Workflow and tools

- Prefer CLI tools over MCP when both can accomplish the same task.
- Batch independent operations.
- Check existing context before rereading files.
- Use debugging output first when logs can explain the issue.
- Verify whether the feature already exists before creating it.
